# ai-digest backend dependencies (Python 3.13)
# Pinned to a sane modern set covering the full stack.

# --- Web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- HTTP client (all outbound calls; intermittent network => tenacity retry) ---
httpx==0.28.1
tenacity==9.0.0

# --- Data models / config ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Postgres + pgvector (psycopg v3, binary build + pool; pgvector type adapter) ---
psycopg[binary,pool]==3.2.3
pgvector==0.4.2

# --- Ingestion ---
feedparser==6.0.11
python-dateutil==2.9.0.post0

# --- Config / numerics ---
pyyaml==6.0.2
numpy==2.2.1

# --- Optional observability (uncomment to enable Langfuse tracing; no-op without it) ---
# langfuse>=2.0

# --- Testing / quality ---
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-httpx==0.35.0
pytest-cov==6.0.0
ruff==0.8.6
mypy==1.14.1
types-PyYAML==6.0.12.20241230
types-python-dateutil==2.9.0.20241206
